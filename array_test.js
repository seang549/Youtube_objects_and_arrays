let data = [
    [1, 2, 3],
    [4, 5, 6],
    [7, 8, 9]
  ];

  // how would we be able to  access the number 8

console.log(data[2][1])

  let numbers = [1, 2, 3, 4, 5];

  //what do we need to do to get our numbers array to return [6, 2, 3]

numbers.pop();
numbers.pop();
numbers.shift();
numbers.unshift(6);
console.log(numbers)